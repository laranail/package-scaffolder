<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;
use {{namespace}}\Http\Controllers\{{controller_name}};

Route::prefix('api/{{package}}')->group(function () {
    Route::get('/', [{{controller_name}}::class, 'index']);
});
