<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Package Runtime Configuration
    |--------------------------------------------------------------------------
    |
    | The Package runtime operates by convention over configuration.
    | All features are enabled by default and work out of the box.
    |
    | This config file exists for consistency but is intentionally empty.
    | Package behavior is controlled through the fluent API in your
    | service provider, not through configuration files.
    |
    | Example usage in your package's service provider:
    |
    | $this->package('my-package')
    |     ->hasViews()
    |     ->hasCommands([MyCommand::class])
    |     ->hasMigrations(['create_users_table'])
    |     ->publishesAssets();
    |
    | For scaffolding configuration, see config/scaffolder.php
    |
    */
];
